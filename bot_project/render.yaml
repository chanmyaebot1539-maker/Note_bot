services:
  - type: web
    name: note-bot
    env: python
    rootDir: .
    buildCommand: bash build.sh
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
      - key: MONGODB_URI
        sync: false
